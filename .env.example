# yfinance を使用してデータ取得します（APIキー不要）

# ── EDINET DB（任意） ────────────────────────────────────────
# サービス: https://edinetdb.jp/developers
# APIキーを取得・登録後に設定するとEDINETデータを優先使用します
# （未設定の場合は yfinance にフォールバックします）
EDINET_API_KEY=

# ── DB パス（任意） ──────────────────────────────────────────
# デフォルトは local.db（プロジェクトルート）
# DB_PATH=local.db

# ── 感情分析モード（任意） ────────────────────────────────────
# rule: キーワードベース（デフォルト）
# hybrid: キーワード + モデル（将来対応）
# SENTIMENT_MODE=rule
